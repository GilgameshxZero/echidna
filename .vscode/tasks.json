{
	"version": "2.0.0",
	"tasks": [
		{
			/* Prepares Markdown-ALl-In-One-generated HTML for frontend. */
			"label": "laminate",
			"type": "shell",
			"options": { "cwd": "${workspaceFolder}/build" },
			"presentation": { "reveal": "silent" },

			"command": "make",
			"args": ["laminate"],
			"windows": { "command": "nmake" }
		}
	]
}
